name: Python checks 🐍

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
    paths:
      - "movie-catalog/**"

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder GH Actions step
        run: echo "Hello, Action!"
